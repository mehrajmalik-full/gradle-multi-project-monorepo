# Gradle properties
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m
org.gradle.parallel=true
org.gradle.caching=true

# Java toolchain is configured in build.gradle.kts (Java 21)
# Don't set org.gradle.java.home here - let toolchain handle it

