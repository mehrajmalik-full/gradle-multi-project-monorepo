/*
 * Settings file for the inventory-app application.
 * This defines the composite build dependencies.
 */

rootProject.name = "inventory-app"

// Include library modules as composite builds
includeBuild("../../libs/profile")
